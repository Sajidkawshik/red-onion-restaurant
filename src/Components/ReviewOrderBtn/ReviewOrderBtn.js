import React from 'react';
import './ReviewOrderBtn.css'
import cart from '../Cart/Cart';


const ReviewOrderBtn = () => {
    console.log(cart);
    console.log(cart.length);

    return (
        <div>
            
        </div>
        
    );
};

export default ReviewOrderBtn;